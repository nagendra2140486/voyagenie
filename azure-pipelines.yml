trigger: none

parameters:
- name: payload
  type: string
  default: ''

pool:
  vmImage: 'ubuntu-latest'

steps:

- checkout: self

- task: UseNode@1
  displayName: 'Install Node.js'
  inputs:
    version: '20.x'

- script: |
    npm ci
  workingDirectory: tests
  displayName: 'Install Dependencies'

- script: |
    npx playwright install --with-deps
  workingDirectory: tests
  displayName: 'Install Playwright Browsers'

#
# Save incoming Impact Gap Analyzer payload
#

- script: |
    printf '%s' "$PAYLOAD" > payload.json

    echo "Payload saved"

    echo "Payload size:"
    wc -c payload.json

  workingDirectory: tests
  displayName: 'Save Incoming Payload'

  env:
    PAYLOAD: ${{ parameters.payload }}

#
# Debug payload
#

- script: |
    echo "Payload received"

    cat payload.json

  workingDirectory: tests
  displayName: 'View Payload'

#
# Dynamic execution
#

- script: |
    node execute-impacted-tests.js
  workingDirectory: tests
  displayName: 'Execute Impacted Tests'

#
# Verify generated files
#

- script: |
    echo "Generated files"

    ls -la

    echo ""
    echo "Regression Report"

    cat regression-report.json

  workingDirectory: tests
  displayName: 'Verify Generated Report'
  condition: always()

#
# Publish report to Claude API
#

- script: |
    echo "Publishing regression report..."

    RESPONSE=$(curl -s \
      --location \
      --request POST \
      --header "Content-Type: application/json" \
      --data @regression-report.json \
      --write-out "\nHTTP_STATUS:%{http_code}" \
      "https://claude-agent-services-e9g8gzh8d3fgbxe6.canadacentral-01.azurewebsites.net/api/publish")

    echo "$RESPONSE"

    HTTP_STATUS=$(echo "$RESPONSE" | grep HTTP_STATUS | cut -d: -f2)

    echo "HTTP STATUS = $HTTP_STATUS"

    if [ "$HTTP_STATUS" -ge 400 ]; then
      echo "Publish API failed"
      exit 1
    fi

  workingDirectory: tests
  displayName: 'Publish Regression Report'
  condition: always()

#
# Publish artifacts
#

- task: PublishPipelineArtifact@1
  displayName: 'Publish Regression Report'
  condition: always()
  inputs:
    targetPath: 'tests/regression-report.json'
    artifact: 'regression-report'

- task: PublishPipelineArtifact@1
  displayName: 'Publish Playwright Results'
  condition: always()
  inputs:
    targetPath: 'tests/results.json'
    artifact: 'playwright-results'

- task: PublishPipelineArtifact@1
  displayName: 'Publish Test Evidence'
  condition: always()
  inputs:
    targetPath: 'tests/test-results'
    artifact: 'test-results'